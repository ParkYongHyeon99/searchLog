<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzzpro.zzz.wan.ExDao">
	
	<select id="linePick">
		SELECT teamPosition,
		concat(ROUND((COUNT(*) / (SELECT COUNT(*) FROM zzz_datas_personal1 WHERE championName =
		#{champions}) * 100), 1),'%') AS linePick
		FROM zzz_datas_personal1 WHERE championName = #{champions}
		GROUP BY teamPosition ORDER BY COUNT(*) DESC LIMIT 3
	</select>
	
	<select id="test">
		select championName,championName_kr
		from champList where line like #{line} order by championName_kr
	</select>
	
	<select id="counter">
		SELECT championName_kr, enemy_champ, 
		COUNT(CASE WHEN win = 1 THEN 1 END) AS win_count, COUNT(*) AS total_count, 
    	round(((COUNT(CASE WHEN win = 1 THEN 1 END) / COUNT(*)) * 100),2) 
    	AS win_rate
		FROM zzz_datas_counter2
		WHERE championName_kr = '가렌' and teamPosition = 'top'
		GROUP BY enemy_champ
		ORDER BY total_count desc limit 1;
	</select>
	
</mapper>