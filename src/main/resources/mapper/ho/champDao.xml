<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzzpro.zzz.ho.dao.ChampDao">
<select id ="winrate">
	SELECT round((COUNT(CASE WHEN win ='True' THEN 1 END) / COUNT(*) * 100),2) AS win_percentage
	 FROM zzz_datas1
	 WHERE championName = #{championName} and teamPosition = #{teamPosition}
</select>


</mapper>